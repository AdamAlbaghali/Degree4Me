CREATE TABLE USER_RESULTS(
    ID BIGINT,
    R_ID SMALLINT DEFAULT 1000,
    RESULT VARCHAR(10000),
    DATE_TIME DATETIME,
    PRIMARY KEY (R_ID),
    FOREIGN KEY (ID)  REFERENCES  USER_INFO(ID)
);


